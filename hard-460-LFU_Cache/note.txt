要解决的问题主要有两个点：1. O(1)时间实现查询，2. O(1)时间实现排序，要达到这样的时间效率基本要靠哈希表。 用一个链表（我用数组模拟的）模拟缓存区以提高node的位置调整效率，节点按频率排序； 用两个哈希表分别存储(key, node)映射和(frequence, node)映射，键值映射不用多说，关键在于频率映射，每次put和get后frequence要加1，然后查找频率哈希表找到当前frequence对应的node0，将当前node移动到该node0后面，然后让频率映射指向当前node。