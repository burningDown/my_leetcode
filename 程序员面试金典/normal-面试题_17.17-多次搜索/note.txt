想到了之前做过的一道类似的题，方法是滑动窗口，但这里显然不适用；
暂时没有什么好办法，于是直接暴力搜，但对于big字符串中每个字符的位置存入一个map中妄图提高效率，然而还是超时，时间复杂度约O(m'nx)，其中m'为big中某一个字符出现的次数，n为smalls长度，x为smalls中一个字符串的长度；

瞄了一眼题解，提到了Trie树，于是把smalls构造成Trie树，然后一边遍历big字符串一边查找Trie树，通过，时间复杂度约为O(mx)，其中m为big的长度，x为smalls中一个字符串的长度。