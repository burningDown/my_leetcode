滑动窗口，但算法本身的思路并不是很难。

先遍历t字符串，用哈希表统计所有的字符个数，并记录有几种字符；
然后滑窗遍历s字符串，窗口指针i和j，j向右遍历，每次遍历判断一下s[j]是否在t字符串中，然后判断当前s[j]的统计个数是否已经覆盖了t中的s[j]字符的个数，若已覆盖则当前字符种类计数currentCount加一；
再判断若currentCount等于t字符串中字符种类的计数count则表示当前子串已经覆盖了t字符串中所有的字符，此时循环将窗口左指针i向右遍历，减去s[i]字符，直到找到最小子串。