这题神烦。。。

oj运行代码的时候是先调用serialize然后接着调用deserialize将返回的结果作为参数传入，所以具体的序列化格式可以自己指定。。。。

我写的第一个版本策略是先将树按照二叉树的顺序表示法存储，即将二叉树根据完全二叉树编号。思路没问题，但最后一个测试用例超时，因为当极端情况下二叉树的所有节点都位于右子节点时，序列化的结果将过于庞大。

于是第二个版本是直接按照bfs遍历顺序来序列化，全部通过。


note：当然还有一个骚操作是执行serialize的时候不序列化而是直接将树储存下来，待调用deserialize的时候直接返回。