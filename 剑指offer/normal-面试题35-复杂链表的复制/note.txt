第一反应是将所有的节点映射保存到一个哈希表中，形成(old_node, new_node)这样的映射，可以解决。

但查看题解后，明白这种题应尽可能避免采用直接哈希表存储的方式。一个比较典型的思路便是链表的原地修改，需要对链表进行三次遍历：
	1. 遍历链表，创建一个当前节点的复制并接到当前节点的后面；
	2. 再次遍历链表，以old_node为当前节点，则next为当前节点的复制，则new节点的random节点为当前old节点的random指向的节点的下一节点；
	3. 再次遍历链表，将old_node和new_node分离出来，形成一个复制的新链表。