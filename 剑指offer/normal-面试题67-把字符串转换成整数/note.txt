简化状态机。

注意溢出边界的处理，因为题目要求只能存储32位整数，故要对越界进行两次判断：
	1. res*10 > INT_MAX时，一定越界；
	2. res*10 == INT_MAX时，要看当前字符值，如果i > INT_MAX - res*10则越界，否则不越界。
	注意为负的情况，若结果为-INT_MAX则并不越界，而上述判断条件下res = INT_MAX时会顺利跳出循环，并返回-INT_MAX，故符合上述条件。