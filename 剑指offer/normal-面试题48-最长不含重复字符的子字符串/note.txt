滑窗动态规划，与之前腾讯面试遇到的那道题有相同的思路。

维护两个指针作为一个窗口，则在遍历过程中，窗口中的所有值都在set中，当窗口左边界j右移后判断当前值是否在set中，若是则将窗口的左边界i右移并从set中删掉这个值，直到j所在的值被删掉。

改进：我们在记录j走过的痕迹时可以同时记录该字符被j记录时的下标，可维护一个map，如果当检测到j所在的字符在map中的下标大于i，则表示该值已在窗口中存在，令i为这个下标值的下一个，然后继续遍历j。